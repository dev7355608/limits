/**
 * Minimum.
 * @param {number} x
 * @param {number} y
 * @returns {number}
 */
export function min(x, y) {
    return x < y ? x : y;
}

/**
 * Maximum.
 * @param {number} x
 * @param {number} y
 * @returns {number}
 */
export function max(x, y) {
    return x > y ? x : y;
}
